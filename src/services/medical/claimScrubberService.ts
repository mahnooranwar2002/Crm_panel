// import { apiClient } from '@/src/api/client';

// // Claim Scrubber Service - Pre-submission claim validation and scrubbing
// export const claimScrubberService = {
//   // Scrub and validate claim before submission
//   scrubClaim: async (claimId: string) => {
//     try {
//       const response = await apiClient.post(`/api/medical/claims/${claimId}/scrub`);
//       return response.data;
//     } catch (error) {
//       throw error;
//     }
//   },
// };

